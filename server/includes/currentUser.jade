//
   Created by laurentMac on 1/29/15.

// bootstrappedUser is only available on the server and at the time that it
  interprets the JADE file and turns the template into actual html

if !!bootstrappedUser
  script.
    window.bootstrappedUserObject = !{JSON.stringify(bootstrappedUser)}